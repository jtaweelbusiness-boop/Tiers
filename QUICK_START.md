# دليل البدء السريع - متجر الإطارات

## 🚀 تشغيل التطبيق

```bash
# تثبيت المكتبات
flutter pub get

# تشغيل التطبيق على Chrome
flutter run -d chrome

# تشغيل التطبيق على Windows (يتطلب Visual Studio)
flutter run -d windows
```

## 👤 بيانات الدخول

### حساب الإدمن الافتراضي:
```
البريد الإلكتروني: admin@tireshop.com
كلمة المرور: admin123
```

### إنشاء حساب مستخدم جديد:
- اضغط على "إنشاء حساب جديد"
- املأ البيانات المطلوبة
- سيتم إنشاء حساب مستخدم عادي

## 🔧 الوظائف الرئيسية

### للإدمن:
1. **الإحصائيات**: عرض إحصائيات شاملة للمتجر
2. **إدارة الإطارات**: إضافة/تعديل/حذف الإطارات
3. **إدارة الطلبات**: متابعة وتحديث حالة الطلبات
4. **إدارة المستخدمين**: عرض بيانات المستخدمين

### للمستخدمين:
1. **تصفح الإطارات**: البحث والتصفية
2. **السلة**: إضافة الإطارات والخدمات
3. **الطلبات**: طلب التوصيل والتركيب
4. **الملف الشخصي**: إدارة البيانات الشخصية

## 📊 البيانات التجريبية

التطبيق يحتوي على:
- ✅ 5 إطارات تجريبية
- ✅ حساب إدمن جاهز
- ✅ تصنيفات متنوعة
- ✅ خدمات التوصيل والتركيب

## 🛠️ استكشاف الأخطاء

### مشكلة Visual Studio:
```bash
# إذا لم يعمل على Windows، استخدم Chrome
flutter run -d chrome
```

### مشكلة قاعدة البيانات:
- قاعدة البيانات تُنشأ تلقائياً عند أول تشغيل
- البيانات التجريبية تُضاف تلقائياً

### مشكلة المكتبات:
```bash
# إعادة تثبيت المكتبات
flutter clean
flutter pub get
```

## 📱 اختبار النظام

1. **تسجيل الدخول كإدمن**
2. **إضافة إطار جديد**
3. **إنشاء حساب مستخدم**
4. **تصفح الإطارات كمستخدم**
5. **إضافة طلب جديد**
6. **تحديث حالة الطلب كإدمن**

---

🎉 **مبروك! النظام جاهز للاستخدام**